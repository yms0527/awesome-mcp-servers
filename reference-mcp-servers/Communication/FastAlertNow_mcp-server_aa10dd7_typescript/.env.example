# Server Configuration
PORT=3000
HOST=localhost
BASE_URL=http://localhost:3000

# Frontend URL (for OAuth redirects)
FRONT_URL=https://fastalert.now

# FastAlert API Configuration
API_URL=https://api.fastalert.now/api

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:6274,http://localhost:3000,https://fastalert.now


# Token Validation
# The server supports two modes:
# 1. Local validation: Tokens issued by this server are validated locally
# 2. External validation: Tokens from external sources are passed through to FastAlert API
# The API_URL/token/validate endpoint can be used for explicit external validation

# Environment
NODE_ENV=development
