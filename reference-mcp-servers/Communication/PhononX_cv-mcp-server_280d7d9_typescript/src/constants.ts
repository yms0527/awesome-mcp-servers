import packageJson from '../package.json';

export const SERVICE_NAME = 'Carbon Voice MCP Server';
export const CV_API_BASE_URL = 'https://api.carbonvoice.app';
export const SERVICE_VERSION = packageJson.version;
export const LOG_DIR = '/tmp/cv-mcp-server/logs';
