# 实施计划

- [x] 1. 补充目录访问地址标准化需求
  - 编写 `uploadFiles` 目录 URL 尾部 `/` 的需求与验收标准
  - 覆盖工具返回值与部署通知一致性要求
  - _需求: 需求 1, 需求 2_

- [x] 2. 设计访问地址规范化方案
  - 明确目录/文件识别规则与 URL 拼接策略
  - 约束根目录、子目录、单文件三类场景行为
  - _需求: 需求 1, 需求 2_

- [x] 3. 实现 `uploadFiles` 访问地址规范化
  - 在 `mcp/src/tools/hosting.ts` 中新增辅助函数
  - 替换现有 `accessUrl` 直拼逻辑
  - _需求: 需求 1, 需求 2_

- [x] 4. 验证构建与行为
  - 运行 `mcp` 包构建验证类型与编译
  - 检查目录 URL 尾部 `/` 行为是否符合预期
  - _需求: 需求 1, 需求 2_
