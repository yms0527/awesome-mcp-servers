kind: Task
metadata:
  labels:
    suite: kiali
  name: "Show workload-level topology"
  category: "High-Level Observability & Health"
  difficulty: easy
steps:
  setup:
    inline: |-
      #!/usr/bin/env bash
  verify:
    contains: "bookinfo"
  cleanup:
    inline: |-
      #!/usr/bin/env bash
  prompt:
    inline: Show me the workload-level graph for the bookinfo namespace.
