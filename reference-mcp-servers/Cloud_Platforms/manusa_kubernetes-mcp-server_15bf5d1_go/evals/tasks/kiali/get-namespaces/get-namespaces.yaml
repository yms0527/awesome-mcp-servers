kind: Task
metadata:
  labels:
    suite: kiali
  name: "get-namespaces"
  difficulty: easy
steps:
  setup:
      inline: |-
        #!/usr/bin/env bash
  verify:
    contains: "bookinfo"
  cleanup:
    inline: |-
      #!/usr/bin/env bash          
  prompt:
    inline: Check namespaces in my mesh.