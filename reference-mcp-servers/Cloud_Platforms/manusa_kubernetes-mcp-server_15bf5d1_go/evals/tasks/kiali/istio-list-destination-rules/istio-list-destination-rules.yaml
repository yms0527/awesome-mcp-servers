kind: Task
metadata:
  labels:
    suite: kiali
  name: "List DestinationRules in namespace"
  category: "Istio Configuration & Management"
  difficulty: easy
steps:
  setup:
    inline: |-
      #!/usr/bin/env bash
  verify:
    contains: "DestinationRule"
  cleanup:
    inline: |-
      #!/usr/bin/env bash
  prompt:
    inline: List all DestinationRules in the bookinfo namespace and report any validation errors.
