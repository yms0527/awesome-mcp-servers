# Hoist all packages including electron for build tools (electron-vite)
# Runtime fix for electron module is handled by banner injection in electron.vite.config.ts
shamefully-hoist=true

# Don't hoist better-sqlite3 so each app can have its own native binary
# Electron needs module version 125, MCP server needs module version 131
public-hoist-pattern[]=!better-sqlite3
