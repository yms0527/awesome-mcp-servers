kind: Task
metadata:
  labels:
    suite: kiali
  name: "List services in namespace"
  category: "Resource Inspection"
  difficulty: easy
steps:
  setup:
    inline: |-
      #!/usr/bin/env bash
  verify:
    contains: "reviews"
  cleanup:
    inline: |-
      #!/usr/bin/env bash
  prompt:
    inline: List all services in the bookinfo namespace.
