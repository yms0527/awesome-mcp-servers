/**
 * Checks if a DOM element is considered readable/interactive
 * @param _element - A jsdom Element to check
 * @returns true if the element has aria-label or is a readable/interactive element type
 */
export function isReadable(_element: globalThis.Element): boolean {
	throw new Error(
		"Not implemented: isReadable has been moved to @mcp-browser-kit/core-extension",
	);
}
