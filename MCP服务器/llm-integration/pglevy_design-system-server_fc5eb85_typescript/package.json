{
  "name": "aurora-mcp-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "bin": {
    "design-system": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "build:clean": "rm -rf build && npm run build",
    "test": "npm run build && node test.js",
    "test:config": "npm run build && node build/config.test.js",
    "test:source-manager": "npm run build && node build/source-manager.test.js",
    "test:all": "npm run test:config && npm run test:source-manager"
  },
  "files": [
    "build"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "MCP server for design system documentation",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.4.0",
    "zod": "^3.24.2"
  }
}
